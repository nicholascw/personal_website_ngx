{{ range readDir "static" }}

  {{ if (findRE ".pdf$" .Name) }}

    {{ printf "<url>" | safeHTML }}

      {{ printf "<loc>%s%s</loc>" $.Site.BaseURL .Name | safeHTML }}
      {{ printf "<changefreq>yearly</changefreq>" | safeHTML }}
      {{ printf "<priority>0.3</priority>" | safeHTML }}

    {{ printf "</url>" | safeHTML }}

  {{ end }}

{{ end }}
